.headerWrapper {
  width: 100%;
  height: 65px;
  position: fixed;
  top: 0;
  left: 0;
  background: #fff;
}
.headerContainer{
    display: flex;
    align-items: center;
    justify-content: space-between;

    .signIn{
      color:#1878f3;
      border-color: #1878f3;
    }
    .left{
      display flex;
      justify-content center;
      align-items center;
      img{
        width 100px;
      }
    }
}

